name: Build SageAttention Wheels (Linux)

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      build_profile:
        description: 'Build profile to use'
        required: true
        default: 'stable'
        type: choice
        options:
          - stable      # Both PyTorch versions (2 wheels)
          - sage2-all   # Same as stable (2 wheels)

jobs:
  # Job 1: Validate/Configure
  configure:
    runs-on: ubuntu-latest
    outputs:
      build_profile: ${{ steps.set-profile.outputs.profile }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set build profile
        id: set-profile
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "profile=${{ inputs.build_profile }}" >> $GITHUB_OUTPUT
          elif [[ "${{ github.event_name }}" == "push" ]] && [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            echo "profile=sage2-all" >> $GITHUB_OUTPUT
          elif [[ "${{ github.event_name }}" == "push" ]] && [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "profile=stable" >> $GITHUB_OUTPUT
          else
            echo "profile=default" >> $GITHUB_OUTPUT
          fi
      
      - name: Summary
        run: echo "📦 Build profile ${{ steps.set-profile.outputs.profile }}"

  # Job 2: Generate build matrix dynamically
  generate-matrix:
    needs: configure
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.generate.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate build matrix
        id: generate
        run: |
          profile="${{ needs.configure.outputs.build_profile }}"

          # Define all possible build configurations
          # Format: python_version|pytorch_version|cuda_version|display_name|arch_list
          declare -a all_builds=(
            # PyTorch 2.7 + CUDA 12.8 + Python 3.12 (Excludes 12.1 arch)
            "3.12|2.7.0|12.8.1|PyTorch 2.7.0 + CUDA 12.8 + Python 3.12|7.0;7.5;8.0;8.6;8.7;8.9;9.0;10.0;12.0"
            # PyTorch 2.8 + CUDA 12.9 + Python 3.12 (Includes 12.1 arch)
            "3.12|2.8.0|12.9.1|PyTorch 2.8.0 + CUDA 12.9 + Python 3.12|7.0;7.5;8.0;8.6;8.7;8.9;9.0;10.0;12.0;12.1"
          )

          # Filter builds based on profile
          declare -a selected_builds=()

          case "$profile" in
            "default")
              # Single default build for PRs (latest)
              selected_builds+=("3.12|2.8.0|12.9.1|PyTorch 2.8.0 + CUDA 12.9 + Python 3.12|7.0;7.5;8.0;8.6;8.7;8.9;9.0;10.0;12.0;12.1")
              ;;
            "stable"|"sage2-all")
              # All builds (both PyTorch versions)
              selected_builds=("${all_builds[@]}")
              ;;
            *)
              echo "Unknown profile: $profile"
              exit 1
              ;;
          esac

          # Convert to JSON matrix format
          matrix_json="["
          first=true
          for build in "${selected_builds[@]}"; do
            IFS='|' read -r python pytorch cuda display arch_list <<< "$build"
            if [ "$first" = true ]; then
              first=false
            else
              matrix_json+=","
            fi
            matrix_json+="{\"python\":\"$python\",\"pytorch\":\"$pytorch\",\"cuda\":\"$cuda\",\"display\":\"$display\",\"arch_list\":\"$arch_list\"}"
          done
          matrix_json+="]"

          echo "matrix={\"include\":$matrix_json}" >> $GITHUB_OUTPUT
          echo "Build count: ${#selected_builds[@]}"
          echo "$matrix_json" | jq .

  # Job 3: Build wheels on Linux (Native)
  build-wheels:
    name: Build ${{ matrix.display }}
    needs: [configure, generate-matrix]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.generate-matrix.outputs.matrix) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Setup Python ${{ matrix.python }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Setup CUDA Toolkit
        uses: N-Storm/cuda-toolkit@v0.2.28
        with:
          cuda: ${{ matrix.cuda }}
          use-github-cache: false

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build

      - name: Upgrade pip and install build tools
        run: |
          python -m pip install --upgrade pip setuptools wheel packaging

      - name: Install PyTorch ${{ matrix.pytorch }}
        run: |
          cuda_version="${{ matrix.cuda }}"
          pytorch_version="${{ matrix.pytorch }}"
          
          if [[ "$cuda_version" == "12.6"* ]]; then
            pip install torch==$pytorch_version torchvision --index-url https://download.pytorch.org/whl/cu126
          elif [[ "$cuda_version" == "12.8"* ]]; then
             pip install torch==$pytorch_version torchvision --index-url https://download.pytorch.org/whl/cu128
          else
             pip install torch==$pytorch_version torchvision --index-url https://download.pytorch.org/whl/cu129
          fi

      - name: Install build dependencies
        run: |
          pip install numpy packaging pybind11

      - name: Update pyproject.toml with PyTorch version
        run: python update_pyproject.py
      
      - name: Build wheel (${{ matrix.display }})
        run: |
          python setup.py bdist_wheel
        env:
          TORCH_VERSION: ${{ matrix.pytorch }}
          CUDA_VERSION: ${{ matrix.cuda }}
          TORCH_CUDA_ARCH_LIST: ${{ matrix.arch_list }}
          MAX_JOBS: "4"

      - name: Verify wheel naming
        shell: bash
        run: |
          echo "=== Built wheels ==="
          ls -lh dist/

          # Verify PEP 427 compliant naming
          for wheel in dist/*.whl; do
            filename=$(basename "$wheel")
            echo "Checking wheel: $filename"

            if [[ ! $filename =~ ^sageattention-[0-9]+\.[0-9]+\.[0-9]+-[0-9]+\.[0-9]+-cp[0-9]+-cp[0-9]+-linux_x86_64\.whl$ ]]; then
              echo "❌ Wheel name does not match expected pattern: $filename"
              exit 1
            fi

            # Extract version info from filename
            if [[ $filename =~ sageattention-([0-9]+\.[0-9]+\.[0-9]+)-([0-9]+)\.([0-9]+)-cp([0-9]+) ]]; then
              sage_version="${BASH_REMATCH[1]}"
              torch_version="${BASH_REMATCH[2]}"
              cuda_version="${BASH_REMATCH[3]}"
              python_version="${BASH_REMATCH[4]}"

              echo "  ✅ SageAttention version: $sage_version"
              echo "  ✅ PyTorch version tag: $torch_version (2.$torch_version.0)"
              echo "  ✅ CUDA version tag: $cuda_version (12.$cuda_version)"
              echo "  ✅ Python version: $python_version (3.$python_version)"
            fi
          done

      - name: Test wheel installation
        run: |
          wheel=$(ls dist/*.whl | head -n 1)
          pip install "$wheel"
          python -c "import sageattention; print('✅ SageAttention imported successfully')"

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheel-linux-py${{ matrix.python }}-torch${{ matrix.pytorch }}-cuda${{ matrix.cuda }}
          path: ./dist/*.whl
          retention-days: 7
          compression-level: 0

  # Job 4: Build summary
  build-summary:
    needs: [configure, generate-matrix, build-wheels]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Download all wheel artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheel-linux-*
          path: ./all-wheels
          merge-multiple: false

      - name: Generate build summary
        run: |
          echo "## 🐧 SageAttention Linux Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Build Profile:** ${{ needs.configure.outputs.build_profile }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Python | PyTorch | CUDA | Wheel Name | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|---------|------|------------|--------|" >> $GITHUB_STEP_SUMMARY

          total_wheels=0
          success_count=0
          fail_count=0

          for artifact_dir in ./all-wheels/wheel-linux-*; do
            if [ -d "$artifact_dir" ]; then
              wheel_files=$(find "$artifact_dir" -name "*.whl")
              wheel_count=$(echo "$wheel_files" | grep -c ".whl" || true)

              if [ "$wheel_count" -gt 0 ]; then
                wheel_name=$(basename $(echo "$wheel_files" | head -1))
                status="✅"
                ((success_count++))
                total_wheels=$((total_wheels + wheel_count))

                if [[ $wheel_name =~ sageattention-([0-9.]+)-([0-9]+)\.([0-9]+)-cp([0-9]+) ]]; then
                  torch_ver="2.${BASH_REMATCH[2]}.0"
                  cuda_ver="12.${BASH_REMATCH[3]}"
                  python_ver="3.${BASH_REMATCH[4]}"

                  echo "| $python_ver | $torch_ver | $cuda_ver | \`$wheel_name\` | $status |" >> $GITHUB_STEP_SUMMARY
                fi
              else
                status="❌"
                ((fail_count++))
              fi
            fi
          done

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 Build Statistics" >> $GITHUB_STEP_SUMMARY
          echo "- **Total wheels built:** $total_wheels" >> $GITHUB_STEP_SUMMARY
          echo "- **Successful builds:** $success_count" >> $GITHUB_STEP_SUMMARY

      - name: Check build results
        run: |
          if [ ! -d "./all-wheels" ]; then
             echo "❌ No wheels downloaded. Build might have failed completely."
             exit 1
          fi
          wheel_count=$(find ./all-wheels -name "*.whl" 2>/dev/null | wc -l)
          if [ "$wheel_count" -eq 0 ]; then
            echo "❌ No wheels were built successfully"
            exit 1
          else
            echo "✅ Successfully built $wheel_count wheel(s)"
          fi

  # Job 5: Create GitHub Release (tags only)
  release:
    needs: [configure, build-summary]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all wheels
        uses: actions/download-artifact@v4
        with:
          pattern: wheel-linux-*
          path: ./all-wheels
          merge-multiple: true

      - name: Prepare release
        id: prep
        run: |
          mkdir -p dist
          cp ./all-wheels/*.whl dist/ 2>/dev/null || true
          wheel_count=$(ls -1 dist/*.whl 2>/dev/null | wc -l)
          echo "wheel_count=$wheel_count" >> $GITHUB_OUTPUT
          version=${GITHUB_REF#refs/tags/v}
          echo "version=$version" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "SageAttention ${{ steps.prep.outputs.version }}"
          files: dist/*.whl
          generate_release_notes: true
          body: |
            ## 📦 SageAttention Linux Wheels - ${{ steps.prep.outputs.version }}
            Includes **${{ steps.prep.outputs.wheel_count }} pre-built wheels**.
